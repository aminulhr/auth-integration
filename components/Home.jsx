// import { useContext } from "react";
// import { AuthContext } from "../ContextApi/AuthProvider";

const Home = () => {
  // const authinfo = useContext(AuthContext);
  // console.log(authinfo);
  return (
    <div>
      <h1 className="text-5xl font-extrabold p-5 text-yellow-600 ">
        Home Page for:
      </h1>
    </div>
  );
};

export default Home;
